#En este segundo ejercicio, tendréis que crear un programa que tenga una clase llamada Alumno que tenga como atributos su nombre y su nota. 
#Deberéis de definir los métodos para inicializar sus atributos, imprimirlos y mostrar un mensaje con el resultado de la nota y si ha aprobado o no.

class Alumno:
    def datos(self, nombre, nota):
        self.nombre = nombre
        self.nota = nota

    def imprimir(self):
        print("Nombre: ", self.nombre)
        print("Nota: ", self.nota)

    def resultado(self):
        if self.nota < 6:
            print("Reprobado")
        else:
            print("Aprobado")


alumno1 = Alumno()
alumno2 = Alumno()

alumno1.datos("Juan", 8)
alumno2.datos("Manuel", 4)

alumno1.imprimir()
alumno1.resultado()
print()
alumno2.imprimir()
alumno2.resultado()
